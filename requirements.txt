PySide6>=6.5  # UI/Qt + OpenGL (QOpenGLWidget)

# Dependencias opcionales eliminadas para optimizar tamaño:
# dotenv, requests, numpy, pandas, networkx, matplotlib, pyyaml
# Si agregas nodos que las requieran, podemos reintroducirlas puntualmente.
